// npm install -D tailwindcss postcss autoprefixer

// npx tailwindcss init -p (old tailwind version needed)

// Steps to deploy project in github: 

// make base url in vite config file
// give the homepage property with url value in the package.json
// Write the two scripts of deployment (refer: pedroTech)
// install gh-pages
// then commit
